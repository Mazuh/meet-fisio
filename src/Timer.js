import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export default function Timer() {
  return (
    <Box component="section">
      <Typography>Not implemented: Timer.</Typography>
    </Box>
  );
}
